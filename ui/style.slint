export global AppStyle {
    in property <string> default-font-family: "";
    in property <float> window-opacity: 0.7;

    // Clock
    in property <string> clock-font-family: "FlexRounded";
    in property <int> clock-font-size: 200;
    in property <int> clock-font-weight: 500;
}
